# Finem Discovery

Original research interviews and opportunity synthesis

**Claude Code**

```bash
claude plugin install finem-discovery@finem
```

**Codex**

```bash
codex plugin add finem-core@finem
codex plugin add finem-discovery@finem
```

**Depends on** `finem-core`. Claude Code installs those automatically; Codex does not resolve dependencies, so the Codex commands above install them in order.

| Capability | Role | Originals |
| --- | --- | --- |
| Product discovery | adds to base | 3 |
| Requirements | adds to base | 1 |
| UX | adds to base | 1 |

Native skills: 1. Bundled originals: 5 from 1 source repository (pm), unmodified under `upstream/`. See `NOTICE.md` for licenses and pinned revisions.
