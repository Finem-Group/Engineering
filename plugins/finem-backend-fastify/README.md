# Finem Backend fastify

Fastify request lifecycle, schema and plugin isolation

**Claude Code**

```bash
claude plugin install finem-backend-fastify@finem
```

**Codex**

```bash
codex plugin add finem-core@finem
codex plugin add finem-backend-fastify@finem
```

**Depends on** `finem-core`. Claude Code installs those automatically; Codex does not resolve dependencies, so the Codex commands above install them in order.

| Capability | Role | Originals |
| --- | --- | --- |
| Backend | adds to base | 1 |
| API contracts | adds to base | 1 |
| Testing | adds to base | 1 |

Native skills: 1. Bundled originals: 1 from 1 source repository (fastify), unmodified under `upstream/`. See `NOTICE.md` for licenses and pinned revisions.
