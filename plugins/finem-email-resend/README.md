# Finem Email resend

Resend transactional email API and webhook handling

**Claude Code**

```bash
claude plugin install finem-email-resend@finem
```

**Codex**

```bash
codex plugin add finem-core@finem
codex plugin add finem-email-resend@finem
```

**Depends on** `finem-core`. Claude Code installs those automatically; Codex does not resolve dependencies, so the Codex commands above install them in order.

| Capability | Role | Originals |
| --- | --- | --- |
| Backend | base | 1 |
| Async/jobs/events | base | 1 |

Native skills: 1. Bundled originals: 1 from 1 source repository (resend), unmodified under `upstream/`. See `NOTICE.md` for licenses and pinned revisions.
