# Finem Redux toolkit

Redux Toolkit state ownership and RTK Query

**Claude Code**

```bash
claude plugin install finem-redux-toolkit@finem
```

**Codex**

```bash
codex plugin add finem-core@finem
codex plugin add finem-redux-toolkit@finem
```

**Depends on** `finem-core`. Claude Code installs those automatically; Codex does not resolve dependencies, so the Codex commands above install them in order.

| Capability | Role | Originals |
| --- | --- | --- |
| Frontend | base | 3 |

Native skills: 1. Bundled originals: 3 from 1 source repository (redux-toolkit), unmodified under `upstream/`. See `NOTICE.md` for licenses and pinned revisions.
