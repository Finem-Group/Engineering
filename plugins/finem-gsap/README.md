# Finem Gsap

GSAP API, lifecycle and performance specialists

**Claude Code**

```bash
claude plugin install finem-gsap@finem
```

**Codex**

```bash
codex plugin add finem-core@finem
codex plugin add finem-gsap@finem
```

**Depends on** `finem-core`. Claude Code installs those automatically; Codex does not resolve dependencies, so the Codex commands above install them in order.

| Capability | Role | Originals |
| --- | --- | --- |
| Frontend | base | 4 |
| Performance | base | 1 |

Native skills: 1. Bundled originals: 4 from 1 source repository (gsap), unmodified under `upstream/`. See `NOTICE.md` for licenses and pinned revisions.
