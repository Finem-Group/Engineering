# Finem Expo

Expo structure, native UI, routing, motion and data fetching

**Claude Code**

```bash
claude plugin install finem-expo@finem
```

**Codex**

```bash
codex plugin add finem-core@finem
codex plugin add finem-expo@finem
```

**Depends on** `finem-core`. Claude Code installs those automatically; Codex does not resolve dependencies, so the Codex commands above install them in order.

| Capability | Role | Originals |
| --- | --- | --- |
| Frontend | replaces base | 7 |
| Performance | replaces base | 1 |
| Design system | base | 1 |

Native skills: 1. Bundled originals: 8 from 2 source repositories (addy, expo), unmodified under `upstream/`. See `NOTICE.md` for licenses and pinned revisions.
