# Finem Svelte

Svelte 5 and SvelteKit implementation and analysis

**Claude Code**

```bash
claude plugin install finem-svelte@finem
```

**Codex**

```bash
codex plugin add finem-core@finem
codex plugin add finem-svelte@finem
```

**Depends on** `finem-core`. Claude Code installs those automatically; Codex does not resolve dependencies, so the Codex commands above install them in order.

| Capability | Role | Originals |
| --- | --- | --- |
| Frontend | replaces base | 3 |
| Performance | replaces base | 1 |

Native skills: 1. Bundled originals: 4 from 2 source repositories (addy, svelte), unmodified under `upstream/`. See `NOTICE.md` for licenses and pinned revisions.
