# Finem Tanstack router

TanStack React Router and Query composition

**Claude Code**

```bash
claude plugin install finem-tanstack-router@finem
```

**Codex**

```bash
codex plugin add finem-core@finem
codex plugin add finem-tanstack-router@finem
```

**Depends on** `finem-core`. Claude Code installs those automatically; Codex does not resolve dependencies, so the Codex commands above install them in order.

| Capability | Role | Originals |
| --- | --- | --- |
| Frontend | adds to base | 3 |

Native skills: 1. Bundled originals: 3 from 1 source repository (tanstack-router), unmodified under `upstream/`. See `NOTICE.md` for licenses and pinned revisions.
