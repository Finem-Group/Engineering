# Finem Search upstash

Upstash Search indexing and retrieval

**Claude Code**

```bash
claude plugin install finem-search-upstash@finem
```

**Codex**

```bash
codex plugin add finem-core@finem
codex plugin add finem-search-upstash@finem
```

**Depends on** `finem-core`. Claude Code installs those automatically; Codex does not resolve dependencies, so the Codex commands above install them in order.

| Capability | Role | Originals |
| --- | --- | --- |
| Backend | base | 1 |

Native skills: 1. Bundled originals: 1 from 1 source repository (upstash), unmodified under `upstream/`. See `NOTICE.md` for licenses and pinned revisions.
