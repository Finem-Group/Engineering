# Finem Tanstack table

TanStack Table 9 core and React table bindings

**Claude Code**

```bash
claude plugin install finem-tanstack-table@finem
```

**Codex**

```bash
codex plugin add finem-core@finem
codex plugin add finem-tanstack-table@finem
```

**Depends on** `finem-core`. Claude Code installs those automatically; Codex does not resolve dependencies, so the Codex commands above install them in order.

| Capability | Role | Originals |
| --- | --- | --- |
| Frontend | base | 3 |

Native skills: 1. Bundled originals: 3 from 1 source repository (tanstack-table), unmodified under `upstream/`. See `NOTICE.md` for licenses and pinned revisions.
